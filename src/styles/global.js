import { StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  headLogoTittle: {
    height: 30,
    width: 80,
  },
  headerRight: {
    flexDirection: 'row',
    alignItems: 'center',
    right: 10,
  },
  headerRightButton: {
    paddingHorizontal: 5,
  }
})

export default styles